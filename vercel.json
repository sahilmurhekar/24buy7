{
    "version": 2,
    "builds": [
      {
        "src": "src/api/*.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/adddata", "dest": "/api/server.js" },
      { "src": "/.*", "dest": "/api/server.js" }
    ]
  }
  